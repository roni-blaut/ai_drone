# ── GPU (CUDA) install — run this BEFORE pip install -r requirements.txt ──────
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Replace cu121 with your CUDA version:  cu118  cu121  cu124
# Check your CUDA version: nvidia-smi   (top-right corner shows "CUDA Version: X.Y")
# ─────────────────────────────────────────────────────────────────────────────

ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.10.0
opencv-python>=4.8.0
Pillow>=9.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.28.0
gdown>=4.7.0
